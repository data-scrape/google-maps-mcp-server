[project]
name = "google-maps-mcp-server"
version = "1.0.0"
description = "Google Maps MCP Server - AI agent access to local business data via MCP"
requires-python = ">=3.12"
dependencies = ["mcp>=1.0.0"]

[project.scripts]
google-maps-mcp-server = "scraper:main"
